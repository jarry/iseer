#if(${appSortList})
{
  'status' : 0,
  'data' : {
           'result' : [
           #foreach( $appSort in $appSortList)
             #if( $velocityCount > 1 ),
             #end
           {
                'id' : '$vu.escapeNull($appSort.name)',
                'title' : '$vu.escapeNull($appSort.displayName)',
                'icon' : '$vu.escapeNull($appSort.icon)'
               }
           #end]
  }
}
#end
