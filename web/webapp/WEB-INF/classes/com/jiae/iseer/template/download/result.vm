{
    "state": #if ($imageInfo['name']) true #else false #end, 
    "data": {
        #if ($imageInfo['name'])
        "image": {
            "name": "$imageInfo['name']",
            "width": $imageInfo["width"],
            "height": $imageInfo["height"],
            "type": "$imageInfo['type']"
        }
        #end
    }
}